import { render } from '@/tests/utils'

import { PerfilSection } from './PerfilSection'

describe('PerfilSection Component', () => {
  it('should render correctly', () => {
    render(<PerfilSection />)
  })
})
