import { render } from '@/tests/utils'
import { PersonalDataSection } from './PersonalDataSection'

describe('PersonalDataSection Component', () => {
  it('should render correctly', () => {
    render(<PersonalDataSection />)
  })
})
