import { render } from '@/tests/utils'
import { SchedulingSection } from './SchedulingSection'

describe('SchedulingSection Component', () => {
  it('should render correctly', () => {
    render(<SchedulingSection />)
  })
})
