import { render } from '@/tests/utils'

import { ChildSection } from './ChildSection'

describe('ChildSection Component', () => {
  it('should render correctly', () => {
    render(<ChildSection />)
  })
})
