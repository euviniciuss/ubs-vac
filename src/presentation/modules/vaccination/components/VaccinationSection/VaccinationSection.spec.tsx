import { render } from '@/tests/utils'

import { VaccinationSection } from './VaccinationSection'

describe('VaccinationSection Component', () => {
  it('should render correctly', () => {
    render(<VaccinationSection />)
  })
})
