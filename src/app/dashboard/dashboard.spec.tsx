'use client'

import { describe, test, assert } from 'vitest'

describe('Dashboard Page', () => {
  test('Should be able to see the dashboard page text on screen', () => {
    assert.equal(2, 2)
  })
})
